[pretty]
    hgpatch = "format:# HG changeset patch%n# User %an <%ae>%n# Date %ad%n%s%n%b"
[alias]
    hgpatch = show --format=hgpatch
[bz]
    default-tracker = bugzilla.mozilla.org
    default-product = Core
    default-component = Networking
    username = hurley@todesschaf.org
[mozreview]
    nickname = nwghurley
[cinnabar]
    helper = ~/.mozbuild/git-cinnabar/git-cinnabar-helper
